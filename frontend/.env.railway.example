# Railway frontend service. Name this service `frontend` in the canvas.
# NEXT_PUBLIC_* is baked in at build time — set this before the first successful
# build, or redeploy after it changes.
#
# Deploy the backend service first so RAILWAY_PUBLIC_DOMAIN exists.

NEXT_PUBLIC_API_URL=https://${{backend.RAILWAY_PUBLIC_DOMAIN}}
