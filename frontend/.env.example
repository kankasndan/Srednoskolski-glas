# Local development. Copy to .env.local (Next loads that automatically).
# Production builds: copy .env.production.example → .env.production instead.
# NEXT_PUBLIC_* is inlined at `next build` time.

NEXT_PUBLIC_API_URL=http://localhost:8000
